/**
 * hero-banner -- full-width clickable promotional banner image.
 * Purely visual; styling lives in hero-banner.css. The offer link
 * (second row) is rendered as an invisible overlay over the image.
 */
export default function decorate() {
  // No behavior required -- layout handled entirely in CSS.
}
